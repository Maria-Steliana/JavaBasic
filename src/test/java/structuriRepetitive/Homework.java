package structuriRepetitive;

public class Homework {

    // Faceti o metoda care sa gaseasca un cuvant dintr-o lista si cand il gaseste sa il printeze in consola
    // Lista de fructe, masini, etc (trebuie sa ai un for care sa pargurga lista, si in interiorul forului sa ai o conditie, un if
    // in momentul in care cuvantul din lista este egal cu cuvantul tinta ar trebui sa execute un SOUT si sa printeze cuvantul)
    //
    // Faceti o metoda care sa gaseasca un cuvant dintr-un hash map si cand il gaseste sa il printeze in consola
    // Hash nap de obiecte, animale, etc (la fel si la hashmap)
}
